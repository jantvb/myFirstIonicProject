import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jose.app',
  appName: 'myFirstIonicProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
